14 Tasks COMPLETED!

PRIORITY: OPTIMIZE MARKUP: cache dom seraches on elements at top!
 
15) WebSocket Status 0 and 1 have light up buttons or something.

16) Replace quit with clear, move clear somewhere else

17) Move quit functionality to top of phone: Re-Connect, Disconnect to socket

18) Locking mechanism: add lock option to shutter after login
    when locked create new unlock form
	
19) make custom gif for cursor removal: ie. gun target (x) or something

20) Everytime a message arrives (flash/ glow users icon in loggged in panel)


	// Set up Hex to RGB Conversion
	function hexToR(h) {
		if(h.length == 3){
		  h = h + h;
		}
		return parseInt((cutHex(h)).substring(0,2),16)
	}
	
	function hexToG(h) {
		if(h.length == 3){
		 h = h + h;
		}
		return parseInt((cutHex(h)).substring(2,4),16)
	}
	
	function hexToB(h) {
		if(h.length == 3){
		 h = h + h;
		}
		return parseInt((cutHex(h)).substring(4,6),16)
	}
	
	// Find colors compliment
	function findCompliment(hex){
	
		thisrgb = new Object();
		thisrgb.r= hexToR(hex);
		thisrgb.g= hexToR(hex);
		thisrgb.b= hexToB(hex);
		var temprgb=thisrgb;
		temphsv=RGB2HSV(temprgb);
		temphsv.hue=HueShift(temphsv.hue,180.0);
		temprgb=HSV2RGB(temphsv);
		return "rgb(" + temprgb.r +"," + temprgb.g +","+temprgb.b+")";
		//SetColors(temprgb.r,temprgb.g,temprgb.b,Array('c','2'));
	};